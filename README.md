# ⚽ Football Tournament — kursachgit

Курсовой проект на **Django**, предназначенный для управления футбольным турниром.  
Приложение позволяет вести команды, игроков и матчи, автоматически формировать турнирную таблицу, просматривать статистику и выгружать отчёты.

---

## 📌 Назначение проекта

Данное веб-приложение разработано для автоматизации учёта футбольного турнира.  
Система позволяет:
- управлять командами и их составами
- вести список игроков
- создавать и редактировать матчи
- фиксировать события матчей
- автоматически рассчитывать турнирную таблицу
- просматривать статистику
- формировать отчёты

Проект реализован в рамках курсовой работы.

---

## 🛠️ Используемые технологии

- Python
- Django
- PostgreSQL
- HTML / CSS
- python-dotenv
- Gunicorn
- WhiteNoise

---

## 📁 Структура проекта

kursachgit  
├── kursach/ — настройки Django-проекта  
├── football/ — основное приложение  
│   ├── templates/ — HTML-шаблоны страниц  
│   └── static/ — стили и изображения  
├── dump.backup — резервная копия базы данных PostgreSQL  
├── manage.py — управляющий файл Django  
└── requirements.txt — зависимости проекта  

---

## 🚀 Установка и запуск (локально)

### 1) Клонирование репозитория

git clone https://github.com/pajvel/kursachgit  
cd kursachgit  

### 2) Создание виртуального окружения (Windows PowerShell)

python -m venv .venv  
.\.venv\Scripts\Activate.ps1  

Если PowerShell запрещает запуск скриптов:

Set-ExecutionPolicy -Scope CurrentUser RemoteSigned  

### 3) Установка зависимостей

python -m pip install -U pip  
pip install -r requirements.txt  

### 4) Настройка переменных окружения

Создай файл `.env` в корне проекта (рядом с `manage.py`):

PGDATABASE=cursach  
PGUSER=postgres  
PGPASSWORD=your_password  
PGHOST=localhost  
PGPORT=5432  

DEBUG=1  
SECRET_KEY=dev-secret  
ALLOWED_HOSTS=localhost,127.0.0.1  

### 5) Применение миграций и запуск сервера

python manage.py migrate  
python manage.py runserver  

После запуска приложение будет доступно по адресу:  
http://127.0.0.1:8000/

---

## 🗄️ Восстановление базы данных из дампа

В репозитории присутствует файл `dump.backup`, содержащий резервную копию базы данных PostgreSQL с тестовыми данными.

### Восстановление дампа:

1) Создай пустую базу данных PostgreSQL:

createdb cursach  

2) Восстанови данные из дампа:

pg_restore -d cursach dump.backup  

3) Убедись, что параметры подключения в файле `.env` соответствуют созданной базе данных.

После восстановления дампа приложение будет доступно с уже заполненными данными (команды, игроки, матчи).

---

## ⚽ Основной функционал

### Команды
- просмотр списка команд
- создание, редактирование и удаление команд
- управление составом команды

### Игроки
- добавление и редактирование игроков
- просмотр информации об игроке
- привязка игроков к командам

### Матчи
- создание и редактирование матчей
- добавление событий матча (голы, карточки, замены)
- автоматический подсчёт результата матча

### Турнирная таблица
- автоматический расчёт очков
- учёт побед, ничьих и поражений
- подсчёт разницы мячей

### Статистика
- голы
- ассисты
- жёлтые карточки
- красные карточки
- фильтрация по командам и позициям

### Отчёты
- отчёт по игрокам
- отчёт по командам
- отчёт по матчам
- форматы выгрузки: TXT и Excel (TSV)

---

## 🧪 Полезные команды Django

python manage.py makemigrations  
python manage.py migrate  
python manage.py createsuperuser  
python manage.py test  

---

## 👤 Автор

pajvel  
GitHub: https://github.com/pajvel  

---

Курсовой проект выполнен в учебных целях.
